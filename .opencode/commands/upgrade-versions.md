---
description: Check and upgrade all pinned versions in metadata/versions.nix
agent: version-upgrader
---

Upgrade versions in this repository. The current versions file:

@metadata/versions.nix

Arguments: $ARGUMENTS

If no arguments are given, check ALL entries. If a package name is given (e.g. "squibble", "mealie"), only process that one.
