---
name: kra
root: ~/git
startup_window: dotfiles

# tmux_command: set-option mouse on

windows:
  - r:
      root: ~/
      layout: main-vertical
      panes:
        # - terra:
        #     - set -xg RESTIC_REPOSITORY rest:https://restic.core.terra.fap.no/
        #     - set -xg RESTIC_PASSWORD_FILE ~/.config/restic.p
        #     - |
        #       while true
        #           restic backup $HOME/git $HOME/Sync

        #           if string match -q -- "*AC Power*" (pmset -g ps)
        #             restic forget --prune \
        #               --keep-daily 7 \
        #               --keep-weekly 5 \
        #               --keep-monthly 12 \
        #               --keep-yearly 75
        #             restic check
        #           end

        #           sleep 3600
        #       end
        # - tjoda:
        #     - set -xg RESTIC_REPOSITORY rest:https://restic.core.tjoda.fap.no/
        #     - set -xg RESTIC_PASSWORD_FILE ~/.config/restic.p
        #     - restic backup $HOME/git $HOME/Sync
        #     - |
        #       while true
        #           restic backup $HOME/git $HOME/Sync

        #           if string match -q -- "*AC Power*" (pmset -g ps)
        #             restic forget --prune \
        #               --keep-daily 7 \
        #               --keep-weekly 5 \
        #               --keep-monthly 12 \
        #               --keep-yearly 75
        #             restic check
        #           end

        #           sleep 3600
        #       end
        # jotta backup is now a launchd service via modules/rustic.nix
        # Logs: ~/Library/Logs/rustic-jotta.log
        - htop
  - dotfiles:
      root: ~/git/dotfiles
  - dotfiles:
      root: ~/git/dotfiles
