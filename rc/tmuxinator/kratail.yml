name: kratail
root: ~/git
startup_window: dotfiles

# tmux_command: set-option mouse on

windows:
  - -:
  - dotfiles:
      root: ~/git/dotfiles
  - corp:
      root: ~/git/corp
  - corp:
      root: ~/git/corp
  - tailscale:
      root: ~/git/tailscale
  - tailscale:
      root: ~/git/tailscale
